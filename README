To run the tool in ubuntu, follow the below mentioned steps

01. git clone https://github.com/x73x61x6ex6ax61x79/DomainRecon.git
02. cd DomainRecon
03. git submodule update --init --recursive
04. chmod 777 configure.sh
05. As Eyewitness required the root priviledges, run the shell script using below command. 
sudo ./configure.sh 


Usage : python EnumSubDomain.py -h
